[project]
name = "refresh-service"
version = "0.1.0"
description = "Watches Dualis for new grades and notifies subscribers via SSE / webhook."
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "httpx>=0.28",
    "pydantic-settings>=2.7",
    "python-dotenv>=1.0",
]
